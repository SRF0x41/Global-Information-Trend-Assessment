import sqlite3

c conn = sqlite3.connect("documents.db")
c.execute("""
    CREATE TABLE IF NOT EXISTS documents (
        id INTEGER PRIMARY KEY,
        source TEXT,
        content TEXT,
        metadata TEXT,
        embedding BLOB
    )
""")
c.commit()
c.close()