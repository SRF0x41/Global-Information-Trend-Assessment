{
  "tools": [
    {
      "name": "list_rss_feeds",
      "description": "Returns all available RSS feeds.",
      "parameters": {
        "type": "object",
        "properties": {},
        "required": []
      }
    },
    {
      "name": "fetch_rss_feed",
      "description": "Fetch and parse a specific RSS feed.",
      "parameters": {
        "type": "object",
        "properties": {
          "feed_id": {
            "type": "string"
          },
          "limit": {
            "type": "integer"
          }
        },
        "required": ["feed_id"]
      }
    }
  ]
}